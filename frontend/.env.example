# Frontend – template for other devs. Copy to .env or use .env.development (npm run dev).
# Production: set VITE_API_URL at build time (Docker/Render).
VITE_API_URL=http://localhost:8000
VITE_BASE_PATH=/

