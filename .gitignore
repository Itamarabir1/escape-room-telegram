# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments (only .venv from uv is used; do not commit)
venv
.venv

# Environment – ignore all .env (secrets/config); allow only template files
**/.env
!**/.env.example
!**/.env.sample

# Generated room images (saved locally; remove this line to track them in Git)
backend/room_images/

# Frontend (Vite)
frontend/node_modules/
# frontend/dist/ no longer committed; built in Docker / on Render (remove from Git: git rm -r --cached frontend/dist)
