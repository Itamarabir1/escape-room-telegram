# Backend – copy to .env and fill values (backend/.env is not committed)
TELEGRAM_TOKEN=
# Frontend app URL (game links, CORS). e.g. https://escape-room-telegram.onrender.com
WEBAPP_URL=
# API public URL (Telegram webhook; same as frontend VITE_API_URL). e.g. https://escape-room-telegram-api.onrender.com
VITE_API_URL=
PORT=8000
ENV=production

DATABASE_URL=postgresql://escape_user:escape_pass@postgres:5432/escape_room
REDIS_URL=redis://localhost:6379/0

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
HF_TOKEN=

# Optional – only if using AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-north-1

GAME_SESSION_TTL=86400
