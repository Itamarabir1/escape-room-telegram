# Copy to backend/.env and fill in. Never commit .env to Git.

TELEGRAM_TOKEN=your_bot_token_from_@BotFather

# --- פיתוח מקומי: אל תגדיר WEBAPP_URL (השאר ריק) ---
# הבוט ישתמש ב-polling, והלינק "שחק עכשיו" יפנה ל־http://localhost:8000.
# פתח את הלינק בדפדפן במחשב אחרי "כולם פה, אפשר להתחיל!".

# --- Redis (game session). If unset, in-memory store is used. ---
# מקומי עם Docker: docker-compose מגדיר REDIS_URL=redis://redis:6379/0 – אין צורך כאן.
# Render: הוסף Redis addon; הקוד תומך ב-REDIS_INTERNAL_URL או REDIS_URL.
# REDIS_URL=redis://localhost:6379/0

# --- PostgreSQL (persistent data, optional). ---
# POSTGRES_URL=postgresql://escape_user:escape_pass@localhost:5432/escape_room

# --- Production (Render): הוסף ב-Render Dashboard → Environment ---
# WEBAPP_URL=https://your-service.onrender.com
