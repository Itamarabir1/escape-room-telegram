# Copy to .env and fill in. Never commit .env to Git.

TELEGRAM_TOKEN=8355718209:AAEXiMK2dQerg4WiH0puGGVtuALZzuQGCLU@BotFather

# Production (Render): set this so webhook + Web App button point to your server
# WEBAPP_URL=https://escape-room-telegram.onrender.com

# Local dev: leave WEBAPP_URL empty or commented out → bot uses polling (no webhook).
# The "שחק עכשיו" button will still open the fallback URL; to test Web App locally,
# open in browser: http://localhost:8000/game?game_id=<game_id>
# WEBAPP_URL=
